{# templates/login/index.html.twig #}
{# {% extends 'base.html.twig' %}
 #}
{% block styles %}
  <link rel="stylesheet" href="{{ asset('css/main.css')}}">
{% endblock %}

{% block body %}
    {% if error %}
        <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}

    <section class="login-section pt-120 pb-120">
    <div class="container">
      <div class="row">
        <div class="col-lg-12">
          <div class="login-block text-center">
            <div class="login-block-inner">
              <h3 class="title">Inicia sesion</h3>
              <form class="login-form" action= "{{ path('app_login')}}" method="post">
                <div class="frm-group">
                  <input type="text" name="_username" id="username" placeholder="Correo">
                </div>
                <div class="frm-group">
                  <input type="password" name="_password" id="password" placeholder="Contraseña">
                </div>
                <div class="frm-group">
                  <input type="submit" value="Iniciar sesión">
                </div>
              </form>
              <p><a href="register">Registrate aquí</a></p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>
{% endblock %}